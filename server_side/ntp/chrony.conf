# Use the local system clock as the time source.
# Docker containers share the host kernel clock, so this effectively
# serves the host machine's time without requiring internet access.
local stratum 8

# Allow any client to sync
allow all

driftfile /var/lib/chrony/drift
rtcsync
